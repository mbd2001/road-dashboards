# https://taskfile.dev

version: '3'

vars:
  GREETING: "Placeholder. Add custom cli tests"

tasks:
  default:
    cmds:
      - echo "{{.GREETING}}"
    silent: true

  test:all:
    desc: _
    cmds:
      - task: default

  test:cli:basic:
    desc: test cli work
    cmds:
      - echo road-eval-dashboard_cli dummy_main

